---
docker_apt_arch: >-
  {{ {'x86_64': 'amd64', 'aarch64': 'arm64'}.get(
    ansible_facts["architecture"],
    ansible_facts["architecture"]
  ) }}

docker_packages:
  - docker-ce
  - docker-ce-cli
  - containerd.io
  - docker-buildx-plugin
  - docker-compose-plugin
